{
  "name": "tds-statseditor",
  "version": "1.2.0",
  "description": "",
  "main": "Main.js",
  "scripts": {
    "build": "npm install && npm run copy-assets",
    "copy-assets": "mkdir -p dist && cp -r node_modules dist/ && find . -maxdepth 1 -not -name 'dist' -not -name '.*' -not -name 'node_modules' -exec cp -r {} dist/ \\;",
    "package": "node ./packager"
  },
  "author": "t7ru",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.6",
    "bootstrap-icons": "^1.13.1",
    "crypto-js": "^4.2.0",
    "html2canvas": "^1.4.1",
    "jquery": "^3.7.1"
  },
  "devDependencies": {
    "prettier": "3.5.3"
  }
}
